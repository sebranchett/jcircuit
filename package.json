{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild src/script.js --minify --format=esm --bundle --outdir=dist"
	},
	"dependencies": {
		"gui-circuit-generator": "github:qucat/gui-circuit-generator#feature/netlist"
	},
	"devDependencies": {
		"esbuild": "^0.25.4"
	}
}
