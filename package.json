{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"bundle": "esbuild src/widget.js --minify --format=esm --bundle --outdir=static",
	    "copy": "cp src/widget.html static/widget.html && cp -r node_modules/gui-circuit-generator/assets static/assets",
		"build": "npm run bundle && npm run copy"
	},
	"dependencies": {
		"gui-circuit-generator": "github:qucat/gui-circuit-generator#feature/netlist"
	},
	"devDependencies": {
		"esbuild": "^0.25.4"
	}
}
